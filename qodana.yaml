version: "1.0"
linter: jetbrains/qodana-cdnet:2024.3-eap
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
dotnet:
  solution: Oma.WndwCtrl.sln
bootstrap: |+
  set -eu
  curl -fsSL https://dot.net/v1/dotnet-install.sh | bash -s -- --version
